#include <stdio.h>
struct rectangle {
    int l, b;
};

int main() {
    struct rectangle r;
    scanf("%d %d",&r.l,&r.b);

    printf("Area=%d\n", r.l*r.b);
    printf("Perimeter=%d", 2*(r.l+r.b));
    return 0;
}
