#include <stdio.h>
int main() {
    int a[6]={1,0,2,0,3,0}, n=6, k=0, b[6];

    for(int i=0;i<n;i++)
        if(a[i]!=0)
            b[k++]=a[i];

    while(k<n)
        b[k++]=0;

    for(int i=0;i<n;i++)
        printf("%d ", b[i]);

    return 0;
}
