#include <stdio.h>

int main() {
    int a[2][3]={{1,5,3},{7,2,9}}, max;

    for(int i=0;i<2;i++){
        max=a[i][0];
        for(int j=1;j<3;j++)
            if(a[i][j]>max)
                max=a[i][j];
        printf("Row %d max = %d\n", i+1, max);
    }
    return 0;
}
