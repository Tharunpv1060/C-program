#include <stdio.h>
int main() {
    char s[50];
    int count=1;
    gets(s);
    for(int i=0;s[i];i++){
        if(s[i]==s[i+1])
            count++;
        else{
            printf("%c%d", s[i], count);
            count=1;
        }
    }
    return 0;
}
