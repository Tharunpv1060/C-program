#include <stdio.h>
int main() {
    char s[50];
    int flag;
    gets(s);
    for(int i=0;s[i];i++){
        flag=0;
        for(int j=0;j<i;j++)
            if(s[i]==s[j])
                flag=1;
        if(flag==0)
            printf("%c", s[i]);
    }
    return 0;
}
