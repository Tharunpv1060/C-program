#include <stdio.h>

int main() {
    int a[]={1,2,3,4,5}, n=5, temp;

    temp=a[0];
    for(int i=0;i<n-1;i++)
        a[i]=a[i+1];
    a[n-1]=temp;

    for(int i=0;i<n;i++)
        printf("%d ",a[i]);
    return 0;
}
