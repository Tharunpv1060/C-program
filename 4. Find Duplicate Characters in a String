#include <stdio.h>
#include <string.h>

int main() {
    char s[50];
    int count[256]={0};

    printf("Enter string: ");
    gets(s);

    for(int i=0;s[i];i++)
        count[s[i]]++;

    for(int i=0;i<256;i++)
        if(count[i]>1)
            printf("%c ",i);
    return 0;
}
